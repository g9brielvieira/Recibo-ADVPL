If Select("SX2") == 0
		RPCSetEnv("99", "01")
		Conout("Setup realizado com sucesso!")
	EndIf

  // esse fonte serve para eu conseguir abrir qualquer fonte pelo vscode, nao precisando entrar no protheus
